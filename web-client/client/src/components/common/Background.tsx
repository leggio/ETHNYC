import styled from 'styled-components';
import background from '../../assets/images/squairy_light.png';

const Background = styled.div`
  padding-top: 50px;
  background-image: url(${background});

  position: relative;
  min-height: 100vh;
`;

export default Background;
